import temp

help(temp) #1: #2:

#1: Como resposta no terminal, teremos informações sobre: NAME, DATA, FILE, FUNCTIONS (quando houver) e continuamos como se estivéssemos no VI. rss
#2: Verifique que agora que utilizamos uma DocString no temp.py, obtivemos no NAME a seguinte saída: temp - Ei amigo, estamos dentro do temp.py usando uma DocString!).
#3: Obs: Usando help() em noss main, com uma importação de um arquivo *.py que contenha uma DocString, obteremos no campo NAME a frase ao lado do nome do arquivo importado.
